{% extends 'base.html.twig' %}

{% block page_content %}
    <h1>Type_construction</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_construction.id }}</td>
            </tr>
            <tr>
                <th>Designation</th>
                <td>{{ type_construction.designation }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('type_construction_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('type_construction_edit', { 'id': type_construction.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
